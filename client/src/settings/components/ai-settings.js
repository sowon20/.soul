/**
 * AI Settings Component
 * AI 설정 UI 컴포넌트 (플레이스홀더)
 */

export class AISettings {
  async render(container, apiClient) {
    container.innerHTML = `
      <div class="settings-placeholder">
        <h3>🤖 AI 설정</h3>
        <p>AI 설정 페이지는 곧 구현됩니다.</p>
      </div>
    `;
  }
}
